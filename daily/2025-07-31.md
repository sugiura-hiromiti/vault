---
aliases: [2025-07-31]
tags: [2025-07-31]
---

- [x] ckecks todo today!(@[[2025-07-31]] 7:00) [completion:: 250728]

#### todo-today

- [ ] ckecks todo today!(@[[2025-07-31]] 7:00)


```dataviewjs
const key ='(@[[2025-07-31]]'

let script = await dv.io.load('script/task.js');
script = `const tl = ${script}
.filter(task => !task.completed)
.filter(task => task.text.includes('${key}'));
dv.taskList(tl);`;

dv.executeJs(script);
```

#### notes-today

```dataviewjs
const today = '2025-07-31'
dv.list(
	dv.pages()
		.filter(p => {
			const date = p.file.frontmatter.created.slice(0,11);

			const today = new Date();

			const year = `${today.getYear()}`;
			const month = `${today.getMonth() + 101}`.slice(1, 3);
			const day = `${today.getDate() + 100}`.slice(1, 3);

			const formatted_today = `${year}-${month}-${day}`;

			return date === formatted_today;
			const date = p.file.mday;
			const year = `${date.c.year}`;
			const month = `${date.c.month + 100}`.slice(1, 3);
			const day = `${date.c.day + 100}`.slice(1, 3);

			const formatted_today = `${year}-${month}-${day}`;

			return today === formatted_today;
		})
		.map(p => dv.fileLink(p.file.path, false, p.file.path))
)
```
