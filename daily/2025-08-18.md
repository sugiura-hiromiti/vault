---
aliases: [2025-08-18]
tags: [2025-08-18]
---

#### todo-today

- [x] ckecks todo today!(@[[2025-08-18]] 07:00)

```dataviewjs
const key ='(@[[2025-08-18]]'

let script = await dv.io.load('script/task.js');
script = `const tl = ${script}
.filter(task => !task.completed)
.filter(task => task.text.includes('${key}'));
dv.taskList(tl);`;

dv.executeJs(script);
```

#### notes-today

```dataviewjs
const today = '2025-08-18'
dv.list(
	dv.pages()
		.filter(p => {
			const date = p.file.mday;
			const year = `${date.c.year}`;
			const month = `${date.c.month + 100}`.slice(1, 3);
			const day = `${date.c.day + 100}`.slice(1, 3);

			const formatted_today = `${year}-${month}-${day}`;

			return today === formatted_today;
		})
		.map(p => dv.fileLink(p.file.path, false, p.file.path))
)
```

---

- [github](https://github.com/sugiura-hiromiti)
- Rustが好きです
- 低レイヤーの世界も好きです
- ↑これらが高じてRustでフルスクラッチからOSを書いています
- 関数型言語に興味があります
- 2024/10~新卒でSESの会社にいましたが、派遣先も含め空気感が全く自分に合わず一年で退職..
- 技術に関してはどこまでも成長していきたいのでレベルの高い環境だよ！って自負のある所はお声をかけていただけると嬉しいです
- 学生時代は吉田寮にいました。第一審勝訴までの道のりをリアルタイムで関わっていたのはちょっと自慢です😁
- フットワークはかなり軽いので場所とかリモートかどうかは気にしていません。勤務地南極です！って言われても気にしません
- ↑寧ろ、自分が成長出来そうな環境か・カルチャーは合っているか・技術的にレベルの高い事が出来そうか、という側面を重視しています